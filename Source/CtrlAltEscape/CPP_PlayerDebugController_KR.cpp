// Fill out your copyright notice in the Description page of Project Settings.


#include "CPP_PlayerDebugController_KR.h"
#include "CtrlAltEscapeCharacter.h"

void ACPP_PlayerDebugController_KR::SwitchControllers()
{
    /*
    // Get the controlled Pawn (e.g. Player Character)
    ACtrlAltEscapeCharacter* TheCharacter = Cast<ACtrlAltEscapeCharacter>(GetPawn());
    if (TheCharacter)
    {
        // Unpossess the current controller
        UnPossess();

        // Possess the other controller (e.g., PlayerController2)
        APlayerController* OtherController = GetWorld()->GetFirstPlayerController();
        if (OtherController)
        {
            Possess(OtherController); //! Cannot convert argument 1 from APlayerController to APawn error
        }
        j*/
}
